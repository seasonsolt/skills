# 贡献指南

感谢你改进 Seasonsolt Skills。一次 PR 只解决一个明确问题；较大的新 skill 请先提交 New skill proposal，避免双方在范围上做无效工作。

## 提交前

1. 阅读目标目录的 `SKILL.md`、相邻说明与现有测试。
2. 保持 skill 自包含：从自身目录解析 `scripts/`、`references/`、`assets/`，不要写入个人机器的固定路径。
3. 运行 `python3 scripts/check.py`（快检可用 `python3 scripts/check.py --quick`），并在 PR 中粘贴实际命令与结果；若改动有额外测试，也一并运行。
4. 检查相对链接、示例命令和依赖声明，确保陌生人能在干净环境中复核。

## 来源与许可证

- 新增或移植内容必须写明作者、上游 URL、版本或 commit、许可证与本仓修改。
- 只有你拥有版权、得到明确授权或许可证允许再分发的内容才能提交。不要复制“网上可见但未授权”的 prompt、文档、样本或输出。
- 修改 Vendored 内容时保留其版权和许可证，并说明补丁是否需要回馈上游。
- benchmark、示例和训练材料需记录来源、许可与必要归属；生成内容不能冒充原作者。
- 根许可证目前待确认。提交实质性新内容前，可先开 issue 确认预期许可；独立许可证内容继续遵循其自身条款。

## 隐私与安全

只提交合成、匿名化或明确获准公开的 fixture。不得提交密钥、Cookie、token、会话正文、客户/账号数据、私有草稿、内部仓库内容或可识别个人的本地路径。若测试需要敏感输入，提交生成 fixture 的方法，不提交原始数据。

发现漏洞或秘密时不要放进 issue 或 PR；按 [`SECURITY.md`](SECURITY.md) 私下报告。

## Skill 最小契约

- `SKILL.md` frontmatter 至少包含稳定的 `name` 和能说明触发范围的 `description`。
- 写清何时使用、何时不使用、信任边界、外部依赖和可复现验证方式。
- 非平凡脚本至少保留一个能失败的自动化检查；不要为了“以后可能需要”增加框架或依赖。
- UI 元数据、示例与 skill 行为保持一致；不要声称未实测的平台兼容性。

## PR 内容

PR 需说明：改了什么、为什么、如何验证、来源/许可证是否变化、是否处理过真实用户数据。维护者可能要求缩小范围或补充可复验证据。
